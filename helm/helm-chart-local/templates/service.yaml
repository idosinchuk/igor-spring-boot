apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart-local.fullname" . }}
  labels:
    {{- include "helm-chart-local.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-chart-local.selectorLabels" . | nindent 4 }}
